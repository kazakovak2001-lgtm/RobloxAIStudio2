name: Security SAST and Credential Scan

on:
  pull_request:
  push:
    branches: [release/cutover-1e-candidate]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codeql:
    name: Backend CodeQL Evidence
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    env:
      SECURITY_SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout exact SAST source
        uses: actions/checkout@v4
        with:
          ref: ${{ env.SECURITY_SOURCE_SHA }}
          fetch-depth: 0
          persist-credentials: false
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript
          queries: security-extended
      - name: Analyze JavaScript and TypeScript
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:javascript-typescript
          output: artifacts/security-sast
      - name: Bind SAST evidence to source
        if: always()
        run: |
          mkdir -p artifacts/security-sast
          jq --null-input \
            --arg controlId "SECURITY-2G-C" \
            --arg repository "kazakovak2001-lgtm/RobloxAIStudio2" \
            --arg sourceSha "$SECURITY_SOURCE_SHA" \
            --arg scanner "github-codeql/javascript-typescript" \
            --arg querySuite "security-extended" \
            '{controlId: $controlId, repository: $repository, sourceSha: $sourceSha, scanner: $scanner, language: "javascript-typescript", querySuite: $querySuite, blockingSeverity: "high", blockingConfidence: "high"}' \
            > artifacts/security-sast/evidence.json
      - name: Publish SAST evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-2g-c-codeql-${{ env.SECURITY_SOURCE_SHA }}
          path: artifacts/security-sast
          if-no-files-found: error
          retention-days: 14

  credential-scan:
    name: Gitleaks Evidence
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      SECURITY_SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      SECURITY_BASE_SHA: ${{ github.event.pull_request.base.sha || '8dd23f88a0b23e2eedf5f2c38a36f0c12570cbab' }}
      SECURITY_HISTORY_BOUNDARY: 8dd23f88a0b23e2eedf5f2c38a36f0c12570cbab
      GITLEAKS_VERSION: 8.30.1
    steps:
      - name: Checkout exact credential-scan source
        uses: actions/checkout@v4
        with:
          ref: ${{ env.SECURITY_SOURCE_SHA }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - name: Install exact validation dependencies
        run: npm ci --ignore-scripts
      - name: Validate security policy
        run: npm run validate:security-policy
      - name: Generate exact fingerprint exceptions
        run: |
          mkdir -p artifacts/security-credentials
          jq '.securityPolicy.scannerExceptions' package.json \
            > artifacts/security-credentials/exceptions.json
          jq --raw-output \
            '.securityPolicy.scannerExceptions[] | select(.controlId == "SECURITY-2G-C" and .scanner == "gitleaks") | .fingerprint' \
            package.json > .gitleaksignore
          expected="$(jq '[.securityPolicy.scannerExceptions[] | select(.controlId == "SECURITY-2G-C" and .scanner == "gitleaks")] | length' package.json)"
          actual="$(grep --count . .gitleaksignore || true)"
          test "$actual" -eq "$expected"
      - name: Prepare changed-content scope
        run: |
          mkdir -p artifacts/security-credentials changed-content
          git diff --name-only --diff-filter=ACMR "$SECURITY_BASE_SHA" "$SECURITY_SOURCE_SHA" \
            > artifacts/security-credentials/changed-files.txt
          while IFS= read -r path; do
            [[ -z "$path" ]] && continue
            [[ -f "$path" ]] || continue
            mkdir -p "changed-content/$(dirname "$path")"
            cp "$path" "changed-content/$path"
          done < artifacts/security-credentials/changed-files.txt
      - name: Scan pull-request changed content
        id: changed-scan
        shell: bash
        run: |
          set +e
          docker run --rm \
            -v "$PWD:/workspace" \
            ghcr.io/gitleaks/gitleaks:v${GITLEAKS_VERSION} \
            dir /workspace/changed-content \
            --gitleaks-ignore-path /workspace/.gitleaksignore \
            --redact=100 \
            --no-banner \
            --report-format json \
            --report-path /workspace/artifacts/security-credentials/changed-content.json
          status=$?
          set -e
          test -s artifacts/security-credentials/changed-content.json || printf '[]\n' > artifacts/security-credentials/changed-content.json
          echo "status=$status" >> "$GITHUB_OUTPUT"
      - name: Scan explicit bounded history
        id: history-scan
        shell: bash
        run: |
          test "$(git rev-parse "$SECURITY_HISTORY_BOUNDARY")" = "$SECURITY_HISTORY_BOUNDARY"
          set +e
          docker run --rm \
            -v "$PWD:/workspace" \
            -w /workspace \
            ghcr.io/gitleaks/gitleaks:v${GITLEAKS_VERSION} \
            git . \
            --log-opts="${SECURITY_HISTORY_BOUNDARY}..${SECURITY_SOURCE_SHA}" \
            --gitleaks-ignore-path .gitleaksignore \
            --redact=100 \
            --no-banner \
            --report-format json \
            --report-path artifacts/security-credentials/bounded-history.json
          status=$?
          set -e
          test -s artifacts/security-credentials/bounded-history.json || printf '[]\n' > artifacts/security-credentials/bounded-history.json
          echo "status=$status" >> "$GITHUB_OUTPUT"
      - name: Bind credential evidence to source and scope
        if: always()
        env:
          CHANGED_STATUS: ${{ steps.changed-scan.outputs.status || '1' }}
          HISTORY_STATUS: ${{ steps.history-scan.outputs.status || '1' }}
        run: |
          mkdir -p artifacts/security-credentials
          test -f artifacts/security-credentials/changed-files.txt || : > artifacts/security-credentials/changed-files.txt
          test -f artifacts/security-credentials/changed-content.json || printf '[]\n' > artifacts/security-credentials/changed-content.json
          test -f artifacts/security-credentials/bounded-history.json || printf '[]\n' > artifacts/security-credentials/bounded-history.json
          test -f artifacts/security-credentials/exceptions.json || printf '[]\n' > artifacts/security-credentials/exceptions.json
          changed_files="$(jq --raw-input --slurp 'split("\n") | map(select(length > 0))' artifacts/security-credentials/changed-files.txt)"
          jq --null-input \
            --arg controlId "SECURITY-2G-C" \
            --arg repository "kazakovak2001-lgtm/RobloxAIStudio2" \
            --arg sourceSha "$SECURITY_SOURCE_SHA" \
            --arg baseSha "$SECURITY_BASE_SHA" \
            --arg historyBoundary "$SECURITY_HISTORY_BOUNDARY" \
            --arg scanner "gitleaks" \
            --arg scannerVersion "$GITLEAKS_VERSION" \
            --argjson changedExitCode "$CHANGED_STATUS" \
            --argjson historyExitCode "$HISTORY_STATUS" \
            --argjson changedFiles "$changed_files" \
            --slurpfile exceptions artifacts/security-credentials/exceptions.json \
            '{controlId: $controlId, repository: $repository, sourceSha: $sourceSha, baseSha: $baseSha, historyBoundary: $historyBoundary, scanner: $scanner, scannerVersion: $scannerVersion, pullRequestScope: "changed-content", plaintextMaterialAllowed: false, changedExitCode: $changedExitCode, historyExitCode: $historyExitCode, changedFiles: $changedFiles, exceptions: $exceptions[0]}' \
            > artifacts/security-credentials/evidence.json
      - name: Publish credential evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-2g-c-gitleaks-${{ env.SECURITY_SOURCE_SHA }}
          path: artifacts/security-credentials
          if-no-files-found: error
          retention-days: 14
      - name: Enforce credential threshold
        env:
          CHANGED_STATUS: ${{ steps.changed-scan.outputs.status || '1' }}
          HISTORY_STATUS: ${{ steps.history-scan.outputs.status || '1' }}
        run: |
          test "$CHANGED_STATUS" -eq 0
          test "$HISTORY_STATUS" -eq 0

  security-gate:
    name: Security SAST and Credential Gate
    runs-on: ubuntu-latest
    needs: [codeql, credential-scan]
    steps:
      - run: echo "SAST and credential checks passed"
