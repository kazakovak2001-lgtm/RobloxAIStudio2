# Current Project State

**Last Updated**: August 11, 2026
**Phase**: `SIM-TRUTH-1` complete (backend PR #231 — the second fabricated score, in the simulation path `PLAYTEST-TRUTH-1` recorded and did not fix. `engagementScore` weighted four values from a tick-driven script, two of which were the same quantity counted twice, and a blueprint with no NPCs scored full marks for interacting with nothing. Simulation evidence is now split into observed facts, derived indicators and an explicit `player: { status: "not-observed" }`; no aggregate score or grade exists; a shortfall the simulator's own stride caused is attributed to that stride rather than to the blueprint; and `/lifecycle/tick` keeps caller values as client claims, leaves missing evidence missing and abstains instead of fabricating health or evolution. **No real player engagement, retention, fun, quality or runtime measurement is claimed.** Known limitation: no server-owned simulation, economy or world evidence source is wired to `/lifecycle/tick`, so it cannot progress a game today and says so explicitly); `PLAYTEST-TRUTH-1` complete (backend PR #229 with Frontend PR #47/#48 — `PlaytestEngine` averaged six numbers into an `overallScore` and called anything above eighty `production_ready`, and one of the six added five points when the generated source contained the substring `pcall`. The findings are kept and the arithmetic on top of them is gone: a report states `evidenceKind: "static-analysis"` and that runtime quality was **not measured**, a terminal session reports `qualityScore: null`, and repair decides on outstanding findings. **This removes a fabricated measurement and adds no capability** — nothing runs a Roblox play session, observes a player, simulates input or measures anything at runtime, and `PLAYTEST-2` is untouched and remains `unscoped`); `ASSET-FABRIC-1` complete (backend PR #227 — the asset plan shipped on every generation and nothing checked it; it now has a versioned typed contract, is validated before persistence and is bound by lineage to the design it derives from. **Complete to the contract only**: nothing resolves, uploads or materializes an asset, and validation is **advisory** — a malformed plan is stored unchanged and the generation still passes); `NOVELTY-2` complete (backend PR #225 — the structural fingerprint `NOVELTY-1` records now has a consumer, which it did not before. A generation carries an explicit verdict **when the run produced enough evidence to form one** — `GenerationExecution.novelty` is optional, and it is absent whenever no `GAME_DNA` artifact was recorded, the report could not be read, or the comparison failed. **Absence means the question was not resolved, never that the generation was found `distinct`.** **only exact fingerprint equality counts as a repeat**, and a repaired execution that carries its parent's design forward is named `repair-preserved` rather than reported as an unrelated duplicate, with repair ancestry read from artifact lineage rather than the execution id; **advisory and threshold-free**, and all eight promotion criteria for a similarity threshold are unsatisfied); `NOVELTY-1` complete (backend PR #223 — the first structural comparison between generations rather than between a generation and its own spec; a GameDNA derived from the world model is fingerprinted and compared against the project's earlier ones read from durable storage, and states `compared`, `no-prior-generations`, `prior-without-dna` or `comparison-failed` rather than treating absence of a comparison as novelty; **advisory**, and `NOVELTY-2` is where a gate would live); `AGENT-SAFETY-1` complete (backend PR #221 — every runtime agent declares an Observe/Plan/Propose/Execute authority tier and whether it may delegate; a delegated call is refused before the callee runs and before any provider is reached when the caller may not delegate, when the callee is not pipeline-reachable, or when it outranks the caller, while the platform's own calls stay unrestricted; no agent claims `execute` and validation refuses any that does); `SECURITY-REVIEW-A2` complete (backend PR #216 — the advisory Luau reviewer can no longer report a pass over code it did not read, states `pass`/`finding`/`not_applicable`/`not_inspected`, binds every reviewed script to the content hash of the bytes it read, and gained rules for RemoteFunction handlers, runtime code compilation, client-chosen player identity and client-chosen HTTP targets; **still advisory**, and it satisfies no `SECURITY-REVIEW-B` promotion criterion); `ARTIFACT-CONTRACT-2` complete (backend PR #214 — every newly produced durable artifact carries its own schema version, owning project, canonical content hash, producer identity and dependency lineage, so a validation or security report names the exact bytes it read rather than being assumed to still apply; historical artifacts carry no envelope and are held to none, and no database migration was required because the column is already JSONB); `AGENT-CONTRACT-1` complete (backend PR #212 — one versioned server-owned definition per runtime agent, stating what it produces, whether it may fall back and how hard it retries, with every policy field consulted by execution, pipeline validation or a reconciliation that runs outside the test suite, and `title` carried as a human-readable label that nothing reads; timeout, cost and input-token ceilings are recorded as gaps rather than declared, because nothing enforces them); `WORLD-1C` scoped and blocked ([scope](./WORLD-1C_SCOPE.md) — canonical runtime world ownership must not move until an operator-observed Studio session confirms `WORLD-1B`, because the switch would make an unobserved materializer the only source of the world and remove the Lua safety net that currently guarantees one exists); `WORLD-1B` code complete but not done (backend PR #209 — the world model materialized into Studio as real instances at design time, under `ReplicatedStorage.AIStudioArtifacts` rather than `Workspace`; contract tested, operator-observed Studio acceptance outstanding); `WORLD-1A` complete (backend PR #207 — a semantic world model and the first check that compares two artifacts to each other; non-canonical, unmaterialized, advisory); `PIPELINE-1A` and `PIPELINE-1B` complete (backend PR #203/#205 — the generation plan is server-owned versioned data that a request may only narrow, an execution reports as completed only work an agent actually performed, and what deterministic validation found is a durable typed artifact instead of an exception message); `PROVIDER-1B` and `SECREVIEW-1` complete (backend PR #200/#201 — fallback provenance completeness, and an **advisory** deterministic Luau trust-boundary review that never negates delivery); `ARTIFACT-1` code complete but not done (backend PR #199 — deterministic Studio identity for delivered metadata; the change is plugin Lua, the repository has no Lua test harness, and no Studio-attached run has confirmed that re-exporting replaces the previous instance without touching creator content); `STUDIO-2F-A` remains code complete but not done (backend PR #195/#196/#197 — generated GUI materialization as real Studio instances; runtime validation outstanding, so it does not yet satisfy `STUDIO-2F-B`…`STUDIO-2F-E`); preceded by `PROVIDER-1A` (backend PR #193) and by REPAIR-1 complete across backend and Frontend UI (backend sub-phases 1A/1B/1C plus the Integrate-stage repair panel in Frontend PR #40) — `STUDIO-2F-B`…`STUDIO-2F-E` are named for ordering only and are not yet scoped; `AUTONOMY-3A` remains deferred and unscoped
**Build Status**: Current runtime pair is backend `77976a7818695cfdccdea253794acc0cd3c79b91` (release/cutover-1e-candidate; the SIM-TRUTH-1 merge carried by PR #231, following the PLAYTEST-TRUTH-1 merge `63221e79854be718e75a8582b6a7afe248ecb22a` in PR #229 and the ASSET-FABRIC-1 merge `042ca72d2b6f34af05779bcf8601f441ae93c0c9` in PR #227 and the NOVELTY-2 merge `78c316db594795fa361330301e30933cbbcef2c3` in PR #225 and the NOVELTY-1 merge `514c321d794a66e0f6f5d407ba906ab3e45087d1` in PR #223 and the AGENT-SAFETY-1 merge `f1adac4b9312aa987502c43fd5e32fe47f535ae4` in PR #221 and the SECURITY-REVIEW-A2 merge `3e18460c394b03c2d373c7d1a2e9cd0b74b6f984` in PR #216 and the ARTIFACT-CONTRACT-2 merge `79b1ecfd137dff71e17fcc57e45078a47137fcdf` in PR #214 and the AGENT-CONTRACT-1 merge `ddfa10453d9e2f48bc9e57045d40a8792d3cc297` in PR #212 and the container-collision fix `c42a9b19f2d58fe728e3b978edffb8acd8621c72` carried by PR #210 and the WORLD-1B merge `4fa948b12ebba768ca5e43442dd361d851b905e3` in PR #209 and WORLD-1A `18bdc2cddd8b80dba5c779e51c5f3947cce9484a`, PIPELINE-1B `d7dc84061dd2045e68b4a38f970fe42e7c25abce`, PIPELINE-1A `d3ddcce4408b950dc2ebe1ef2767998f93e83970`, SECREVIEW-1 `12bd4dc6c736aa6af7b3346624152032157d5bd1`, ARTIFACT-1 `2a3fc01a76a59cf7e92233037f96dcb22a9d30c3` and PROVIDER-1B `b1c2283a2d8ae6722b29a7eeca45e177b4eb7ca7`, which in turn followed the last STUDIO-2F-A merge `d7e444549d324df9df339ff5bda7a75a820e39e3`) plus protected Frontend contents `kazakovak2001-lgtm/Frontend@b91ebdba613ca0d8f01a5fb33de355ae97588601` (merged PR #48, which removed the transitional acceptance once the backend member carried PLAYTEST-TRUTH-1, following PR #47 `0a6857ad548b9c3d48019dfa7a63adbb5fde4930` which introduced it, and PR #45, the last of four corrective PRs, #42–#45). **The pinned pair is level with canonical `main`**, restored by `PAIR-ADVANCE-1` after [ROADMAP-RECONCILIATION-1](./ROADMAP-RECONCILIATION-1_RESULT.md) found the pin four PRs behind and advanced again by the `PLAYTEST-TRUTH-1` pin move; backend CI checks out this exact commit, so the pair is verified rather than merely declared. The reciprocal production, clean-clone, PostgreSQL restart, release-image, composed-release, security and Merge Gate chain must remain exact-pair bound. STUDIO-ACCEPT-1 passed on backend acceptance commit `3230d2368ed781043fe9f3520c0d3de3836ec3bb`. A Roblox Studio Play-mode runtime playtest on the RUNTIME-PLAYTEST-1 pair passed with operator-observed evidence recorded in [RUNTIME-PLAYTEST-1_RESULT.md](./RUNTIME-PLAYTEST-1_RESULT.md). REPAIR-1A replaced the simulated repair engine with a real, artifact-applying, single-strategy repair (`regenerate_script`, whole-package regeneration validated by the same playability gate as generation). REPAIR-1B added `POST /api/repair/:projectId/deliver`, which resolves the latest repaired execution server-side and pushes it to a connected Studio client through the existing sync pipeline. REPAIR-1C added a durable delivery/rollback audit trail (`RepairDeliveryRecord[]` on the repair session) and let `/deliver` redeliver an explicit, project-validated older execution instead of only the latest repair, plus `GET /:projectId/deliveries` combining that audit trail with the existing generic Studio evidence. No external production deployment is claimed.

---

## Architecture

### Backend

- **Server**: Express + Socket.io (Node.js/TypeScript)
- **Files**: 635 TypeScript files under `server/src`: 555 production files and 80 test files across 46 real top-level subsystems
- **API Routes**: 30 unique mounted `/api` prefixes (`/api/projects` mounts two routers), plus health and root endpoints
- **AI Providers**: 6 configurable modes (OpenAI, Anthropic, Gemini, Groq, Ollama, OpenRouter), plus no-provider stub behavior and test mocks
- **Storage**: One configured provider per process; PostgreSQL migrations and cache hydration complete before the server listens. `STORAGE_PROVIDER=postgres` requires `DATABASE_URL`.
- **Durable records**: identities, sessions, users, projects, generation history, API keys, blueprints, blueprint versions, generation executions, pipeline artifacts and lifecycle state, autonomous sessions/checkpoints, Studio command/verification evidence, conversations, and conversation messages use the configured storage boundary.
- **Studio runtime**: project sync, plugin registration, artifact snapshots, transfer, the outbound command lifecycle, acknowledgement/result processing, and exact artifact ID/hash verification use one shared Studio v2 runtime. Serializable command and verification evidence is provider-backed; live clients, sockets, timers and callbacks remain explicitly process-local. Project sync selects the newest completed artifact-bearing execution and never creates placeholder Lua/config packages. Queue delivery alone never marks an import verified.
- **Generation-to-Studio boundary**: the existing `GenerationArtifactRecorder` preserves canonical `scripts[]` payloads and normalizes current `LuaGeneratorAgent` server/client/shared/module `{ name, code }` groups into validated Studio `{ path, content }` scripts. Empty, malformed, duplicate-path, placeholder, and non-playable Lua output is rejected before queueing. PR #174 preserves project metadata, repairs one semantically invalid real-provider response, and requires server world/interactivity plus a client HUD before Studio delivery.
- **Canonical Studio plugin**: `studio-plugin/` v1.8 reuses the existing connector, lifecycle manager, sync manager, artifact loader, events, and UI. It connects with the exact backend project ID, polls `EXPORT_PROJECT`, acknowledges delivery, materializes structured Lua scripts and non-Lua metadata as Roblox instances, reports one exact ID/hash receipt per pipeline artifact, and shows Verified only after the backend accepts the evidence. Roblox-owned JSON content type is provided through `Enum.HttpContentType.ApplicationJson`; the plugin does not submit a forbidden custom `Content-Type` header.
- **Studio plugin package**: `npm run studio:package` creates a deterministic installable `.rbxmx`, a source/bundle manifest, and SHA-256 checksums from an explicit active-module allowlist. The dedicated package workflow validates XML structure and checksums before uploading the desktop acceptance artifact.
- **Verified acceptance package**: workflow run `30277078815`, artifact ID `8657228073`, bundle size `45932` bytes, bundle SHA-256 `a97e6268193f202cb5cc12ef5c174d0a028067c382327dd9432aacbe80f5ced7`, manifest SHA-256 `0655ae43b48f8c3bb90591da1e35170ff122136f8352bad73320c88c0eca3f14`.
- **Current STUDIO-ACCEPT-1 package**: local deterministic package from backend `3230d2368ed781043fe9f3520c0d3de3836ec3bb`, bundle size `47969` bytes, bundle SHA-256 `86e102b663d48925f9e313248761bb2d91d7e0794252f6c04e50496d8ba05696`, manifest SHA-256 `4a249efdbf12acf440298992435dc74865673fbe8e357df809edae93b2701dfc`.
- **Real-time**: Socket.io with 50+ event types, project rooms, and storage-backed opaque-session authentication in production
- **Authentication**: bcrypt password hashing (cost 12), storage-backed opaque sessions/roles, cookie-only browser credential delivery, and production REST/Socket validation. Register/login/refresh JSON is credential-free. High-entropy refresh values are stored only as SHA-256 digests with a bounded digest index, migrated before startup traffic, and consumed before rotation; access cookies cover `/` for Socket.IO, refresh cookies are endpoint-scoped, and production policy is `Secure`, `HttpOnly`, `SameSite=Lax`, host-only.

### Canonical Frontend

- **Repository**: [kazakovak2001-lgtm/Frontend](https://github.com/kazakovak2001-lgtm/Frontend) on `main`
- **Acceptance commit**: `a8d005d433d48e18d8e64ac176ee63c9c694b644`, independently matched to the ZIP used during the successful STUDIO-1 session.
- **Initial SSR release commit**: `1036c3ef9705d145cb9700cd14268a33d2abdd58`, merged through Frontend PR #12 after CI run #65 verified the production image, `/health`, SSR `/`, responsive QA, and Merge Gate.
- **Active paired release contents**: `b91ebdba613ca0d8f01a5fb33de355ae97588601`, including the INTEGRATION-1B browser-recovery proof, retained by the protected release inventory.
- **Framework**: React 19 + TypeScript + Vite + Tailwind CSS
- **Routing and state**: TanStack Router/Query, typed backend adapter, Socket.IO realtime client
- **Ownership**: All new user-facing web functionality belongs in the standalone repository.
- **Current Workspace**: `/projects/$projectId` is organized as Define → Generate → Validate → Integrate → Operate. It uses one persisted Workspace read model, one stage-scoped backend tool registry, typed run/context summaries, durable history, existing chat/realtime/Studio contracts, and production responsive QA at 1440 px, 1024 px, and 390 px. No parallel Workspace or duplicate transport layer was introduced.

### Legacy Frontend

- **Historical location**: repository root `src/`
- **Status**: Physically removed by the CLEANUP-1C implementation; its exact 168-file baseline remains recorded in Git history and the cleanup inventory.
- **Canonical replacement**: `kazakovak2001-lgtm/Frontend` remains the only web client.
- **Guard**: The cleanup audit requires the exact removal diff and the architecture validator fails if root `src/` is reintroduced.

### TECH-AUDIT-2 Evidence Baseline

The TECH-AUDIT-2 figures below are the dated July 28 audit baseline, not the
current DATA-202 release counts. Current release evidence is 80 passing backend
test files / 829 passing tests, one skipped file / two skipped PostgreSQL-gated
tests locally, plus a green protected PostgreSQL Restart E2E job.

- Backend: 61 passing test files, 672 passing tests, one skipped test file/test; typecheck, lint, format, build, PostgreSQL restart, release image, composed HTTPS, rollback, and cleanup invariant gates pass.
- Frontend: TypeScript, production build, SSR image, responsive QA, and 12 native Workspace tests pass. The exact 40-check production-mode integration suite is protected by Frontend Merge Gate and records both repository SHAs plus runtime evidence.
- Frontend quality gap: lint reports 640 errors and 12 warnings; 70 files fail a separate Prettier check. Frontend CI currently runs neither gate.
- Architecture gap: the manifest models 32 domains while 46 subsystems exist. The generated report contains four cycles and status `FAIL`, but the current CLI exits successfully.
- Planning baseline: the evidence-scored feature matrix averages 66%; this is a prioritization aid, not a release SLA or substitute for closing P0 findings.

See [Technical Audit v2.0](../02-audits/technical-v2/EXECUTIVE_AUDIT.md) for the complete evidence, limitations, and recommended order.

---

## Completed Phases

| Phase             | Description                                               | Date           |
| ----------------- | --------------------------------------------------------- | -------------- |
| UX-3              | Initial Implementation                                    | July 2026      |
| UX-3A             | Forensic Audit                                            | July 2026      |
| UX-3B             | Architecture Stabilization Planning                       | July 2026      |
| UX-3C             | Execution Planning                                        | July 2026      |
| UX-3D Sprint 1    | Duplicate Consolidation                                   | July 15, 2026  |
| UX-3D Sprint 2    | shared/ui Migration                                       | July 15, 2026  |
| UX-3D Sprint 3    | Repository Structure Execution                            | July 15, 2026  |
| UX-3D Sprint 4    | Import & Path Alias Migration                             | July 15, 2026  |
| UX-3D Sprint 5    | Legacy Cleanup                                            | July 15, 2026  |
| UX-3D Sprint 6    | Design System Enforcement                                 | July 15, 2026  |
| UX-3D Sprint 7    | Documentation Synchronization                             | July 15, 2026  |
| UX-3D Sprint 8    | Final Validation & Closure                                | July 15, 2026  |
| UX-4 Preparation  | Development Readiness                                     | July 15, 2026  |
| UX-4.0            | Product Roadmap Definition                                | July 15, 2026  |
| UX-4 Phase 1      | Must Have Features (F-1, F-2, F-3)                        | July 15, 2026  |
| UX-4 Phase 2A     | Should Have Features (F-4 to F-8)                         | July 15, 2026  |
| UX-4 Phase 2B     | Infrastructure (F-9, F-10, F-11)                          | July 15, 2026  |
| CUTOVER-0         | Standalone frontend governance                            | July 24, 2026  |
| CI-BASELINE-1     | Portable CI and repository hygiene                        | July 24, 2026  |
| CORE-1a           | Durable identity, projects, and ownership                 | July 24, 2026  |
| CORE-1b           | Durable blueprints, executions, chat, restart E2E         | July 24, 2026  |
| WORKSPACE-1       | Workflow Workspace, scoped tools, logic and QA            | July 24, 2026  |
| STUDIO-1a         | Durable canonical generation artifact lineage             | July 24, 2026  |
| STUDIO-1b         | Shared Studio runtime and real artifact queue             | July 24, 2026  |
| STUDIO-1c backend | ACK/result state machine and exact evidence verification  | July 24, 2026  |
| STUDIO-1d plugin  | Canonical plugin command/import contract                  | July 24, 2026  |
| STUDIO-1e package | Deterministic installable plugin and acceptance runbook   | July 25, 2026  |
| STUDIO-1f fixes   | Real desktop transport, artifact, and routing fixes       | July 27, 2026  |
| STUDIO-1g         | Real desktop import and backend verification              | July 27, 2026  |
| CUTOVER-1A        | Backend-only production release artifact                  | July 27, 2026  |
| CUTOVER-1B        | Standalone Frontend SSR release artifact                  | July 27, 2026  |
| CUTOVER-1C        | Composed HTTPS release and authenticated transports       | July 27, 2026  |
| CUTOVER-1D        | Release-baseline readiness and rollback rehearsal         | July 28, 2026  |
| CUTOVER-1E        | Controlled default-branch promotion                       | July 28, 2026  |
| CUTOVER-1F        | Promoted baseline CI alignment                            | July 28, 2026  |
| CLEANUP-1A        | Legacy frontend decommission audit                        | July 28, 2026  |
| CLEANUP-1B        | Legacy frontend tooling decoupling                        | July 28, 2026  |
| CLEANUP-1C        | Legacy frontend physical removal                          | July 28, 2026  |
| CLEANUP-1D        | Post-removal verification and permanent guard             | July 28, 2026  |
| TECH-AUDIT-2      | Two-repository technical and architecture baseline        | July 28, 2026  |
| SEC-201           | Cookie-only browser auth and refresh protection           | July 28, 2026  |
| FE-201            | Real Studio verification in the canonical Workspace       | July 28, 2026  |
| INT-201           | Reciprocal protected 40-check production contract         | July 28, 2026  |
| DOC-201           | Active auth/release authority and terminology guard       | July 28, 2026  |
| DATA-201          | Durable write cutover and compatibility-write removal     | July 31, 2026  |
| DATA-202A         | Pipeline lifecycle persistence and restart truthfulness   | July 31, 2026  |
| DATA-202B         | Autonomous session/checkpoint persistence                 | July 31, 2026  |
| DATA-202C         | Studio evidence persistence and runtime ownership audit   | July 31, 2026  |
| DOC-202A          | Documentation authority inventory and deterministic guard | August 3, 2026 |
| INTEGRATION-1A    | Frontend/backend contract and clean-clone verification    | August 4, 2026 |
| INTEGRATION-1B    | PostgreSQL restart recovery and lifecycle validation      | August 5, 2026 |
| ROADMAP-AUDIT-1   | Evidence-based roadmap reconciliation                     | August 5, 2026 |
| STUDIO-ACCEPT-1   | Real Studio delivery on project-scoped API authority      | August 6, 2026 |

---

## Historical Feature Completion Status

> The table below records the UX-4 delivery history for the embedded frontend. It is not the source of truth for current production completeness or new standalone frontend work. See [FRONTEND_CUTOVER.md](./FRONTEND_CUTOVER.md) and the current [FEATURE_MATRIX.md](../02-audits/technical-v2/FEATURE_MATRIX.md).

| ID   | Feature                  | Status       | Details                                              |
| ---- | ------------------------ | ------------ | ---------------------------------------------------- |
| F-1  | Analytics Real Data      | ✅ Connected | analyticsApi → /api/analytics (7 endpoints)          |
| F-2  | AI Studio Chat Backend   | ✅ Connected | aiEngine → /api/lua, /api/concept (real Lua gen)     |
| F-3  | Plugin Manager Real Data | ✅ Connected | PluginManagerPage → /api/platform (real API data)    |
| F-4  | Game Simulation          | ✅ Connected | simulationApi → /api/simulate (workspace panel)      |
| F-5  | Economy Designer         | ✅ Connected | economyApi → /api/economy (workspace panel)          |
| F-6  | Autonomous Pipeline      | ✅ Connected | autonomousApi → /api/autonomous (5 API fns, polling) |
| F-7  | Knowledge Base UI        | ✅ Connected | knowledgeApi → /api/knowledge (page + 4 endpoints)   |
| F-8  | Playtesting Dashboard    | ✅ Connected | playtestApi → /api/playtest (workspace panel)        |
| F-9  | Multi-Project Workspace  | ✅ Complete  | SaaSProjectRepository + ownership + auth headers     |
| F-10 | Real Authentication      | ✅ Complete  | bcrypt, opaque-session validation, httpOnly cookies  |
| F-11 | Persistent Storage       | ✅ Complete  | PostgreSQL with InMemory fallback                    |

---

## Security Hardening Status (Release Sprint)

| Task | Description                                 | Status                                 |
| ---- | ------------------------------------------- | -------------------------------------- |
| 1    | Bug condition exploration tests             | ✅ Complete                            |
| 2    | Preservation property tests                 | ✅ Complete                            |
| 3    | Auth route PUBLIC_PREFIXES fix              | ✅ Complete                            |
| 4    | bcrypt password hashing (replaces SHA-256)  | ✅ Complete                            |
| 5    | httpOnly cookie delivery                    | ✅ Cookie-only browser responses       |
| 6    | Opaque-session validation in authMiddleware | ✅ Complete                            |
| 7    | Socket.IO opaque-session validation         | ✅ Complete                            |
| 8    | Dead code removal                           | ✅ Historical scope complete           |
| 9    | Documentation synchronization               | ✅ DOC-201 implemented under issue #49 |
| 10   | Production infrastructure                   | ✅ Complete                            |
| 11   | Final verification and release report       | ✅ Historical release scope complete   |
| 12   | Checkpoint — protected backend checks pass  | ✅ Complete                            |

---

## Historical Backend Capabilities — Legacy Frontend Connection Status

> The mapping below describes the embedded frontend. The standalone frontend mapping is governed by the CUTOVER delivery sequence.

| Backend System | Route            | Frontend Status                                      |
| -------------- | ---------------- | ---------------------------------------------------- |
| Analytics      | /api/analytics   | ✅ Connected (analyticsApi, AnalyticsPage)           |
| AI Studio      | /api/lua         | ✅ Connected (aiEngine, AiStudioPage)                |
| Simulation     | /api/simulate    | ✅ Connected (SimulationPanel in Workspace)          |
| Economy        | /api/economy     | ✅ Connected (EconomyPanel in Workspace)             |
| Playtest       | /api/playtest    | ✅ Connected (PlaytestPanel in Workspace)            |
| Knowledge      | /api/knowledge   | ✅ Connected (KnowledgePage at /knowledge)           |
| Autonomous     | /api/autonomous  | ✅ Connected (AutonomousPipelinePanel)               |
| Platform       | /api/platform    | ✅ Connected (Auth login/register/logout/refresh/me) |
| Plugin Mgr     | /api/platform    | ✅ Connected (PluginManagerPage)                     |
| Studio Bridge  | /api/studio      | ✅ Connected (studioBridgeApi)                       |
| World Gen      | /api/world       | No frontend page                                     |
| Repair         | /api/repair      | No frontend page                                     |
| Domain         | /api/domain      | No frontend page                                     |
| Agent Collab   | /api/agents      | No frontend page                                     |
| Distributed    | /api/distributed | No frontend page                                     |
| Lifecycle      | /api/lifecycle   | No frontend page                                     |
| Compile        | /api/compile     | No frontend page                                     |

---

## Known Problems

1. **Runtime playtest authority (P1)**: the current `PlaytestEngine` performs deterministic static analysis and records `runtimeExecuted=false`; it is not a Roblox runtime verdict.
2. **Repair authority (P1)**: manual repair-redeliver-and-rollback now works end-to-end via `POST /api/repair/run`, `POST /api/repair/:projectId/deliver` (optionally with an explicit `executionId` for rollback), and `GET /api/repair/:projectId/deliveries` for the audit trail (REPAIR-1A/1B/1C). The Integrate-stage repair panel (Frontend PR #40) surfaces this capability. The autonomous-phase `RepairAdapter` remains unavailable and is not wired into the autonomous orchestration loop — that is AUTONOMY-3A, not REPAIR-1.
3. **Native delivery breadth (P2)**: asset generation produces validated definitions/placeholders rather than authoritative uploaded Roblox assets, GUI, and complete place delivery.
4. **Autonomous collaboration (P2)**: orchestration and durable sessions exist, but the bounded collaboration path reports `executedTaskCount=0` and remains preview-only.
5. **Studio protocol diagnostics (P2)**: STUDIO-ACCEPT-1 observed one non-blocking startup `/api/studio/protocol/message` 400 response, a missing `rbxassetid://0` toolbar icon, and one post-save `Callbacks cannot yield` message. Each requires independent reproduction before a production change.

Project-scoped Studio API-key authority is resolved by PR #171. The exhaustive
architecture build gate is restored by PR #172. Current desktop evidence is
recorded in `STUDIO-ACCEPT-1_DESKTOP_ACCEPTANCE_RESULT.md`.

---

## Technical Debt

TECH-AUDIT-2 remains historical planning evidence. ROADMAP-AUDIT-1 (#169)
supersedes its ordering after verifying the current code at exact backend and
Frontend baselines. Authoritative Roblox runtime playtest evidence
(RUNTIME-PLAYTEST-1) is complete. Artifact-applying repair, redelivery, and
revalidation (REPAIR-1) is complete on the backend: sub-phase 1A landed a
real single-strategy repair loop (backend PR #185, Frontend companion
PR #38), sub-phase 1B landed Studio redelivery of repaired artifacts
(backend PR #187), and sub-phase 1C landed a durable delivery/rollback
audit trail (backend PR #189). The Frontend UI surfacing repair, delivery,
and rollback landed in Frontend PR #40, completing REPAIR-1 end to end.
Studio sync hardening (#168 and Frontend #32) is
complete, verified against backend PR #176 and Frontend PR #34/#37. Native
asset and GUI delivery is now decomposed as `STUDIO-2F-A`…`STUDIO-2F-E`.
`STUDIO-2F-A` is code complete through backend PR #195/#196/#197 but not
done: runtime validation is a Definition of Done item, nothing in the phase
executed Lua, and the operator-observed acceptance record does not exist yet.
`STUDIO-2F-B`…`STUDIO-2F-E` remain unscoped. Native asset delivery, broader
autonomous agent execution and production observability remain separate
backlog items.

PROVIDER-1A leaves two items deliberately unaddressed and tracked rather than
silently fixed: the provider auto-detection path still defaults to the
hardcoded `llama3` Ollama model, and `REQUIRE_LLM_PROVIDER` ships default-off,
so enabling it in the release image remains an explicit operational decision.
The audit that produced PROVIDER-1A also recorded, without acting on them,
that `PipelineArtifact` identity uses a per-run `randomUUID` as the Studio
object name (so repeated metadata delivery accumulates stale instances),
that `providers/ai/**` and `ai/router.ts` have no importers, and that
`BaseAgent.timeout` is assigned but never enforced.

See [TECHNICAL_DEBT.md](../02-audits/technical-v2/TECHNICAL_DEBT.md) for definitions of done and [SPRINT_BACKLOG.md](../02-audits/technical-v2/SPRINT_BACKLOG.md) for ordered implementation work.

---

## Development Process

All implementation work must follow: `docs/templates/IMPLEMENTATION_TASK_TEMPLATE.md`

This template enforces:

- Pre-implementation verification (reuse before create)
- Consistent validation (backend TypeScript + architecture + tests)
- Mandatory documentation updates
- Definition of Done checklist

---

## Production Infrastructure

- **Backend release image**: `Dockerfile.backend` builds and starts the compiled backend without root `src/`, `public/`, Vite, or Tailwind inputs; CI verifies `GET /health`.
- **Backend/PostgreSQL composition**: `deploy/docker-compose.backend.yml` provides the independently verified backend and persistent database boundary.
- **Standalone Frontend release image**: active paired Frontend contents `b91ebdba613ca0d8f01a5fb33de355ae97588601` package `.output` plus one shared worker-to-Node adapter as a non-root SSR process.
- **Historical REL-202 composed HTTPS evidence**: backend source `010532f0b162097c8a645b1dc07c89081d25cb99` and exact Frontend contents `9495b696cf22c84cf61375f7df22e5ac5907cc3c` passed CI Pipeline #1073 (`30667404383`). The promoted backend merge `a33a8c30588f1e4705d27856e61d839c8efd42ac` has the same file tree. This retained evidence proves the prior DATA-202 pair; the active REL-203 pair is defined by the release inventory and its protected PR chain. External deployment remains a separate unchecked operation.
- **Migration Runner**: `server/src/platform/storage/postgres/migrationRunner.ts` — auto-applies pending migrations on startup (skips when STORAGE_PROVIDER=inmemory).
- **Rollback inventory**: CUTOVER-1A and CUTOVER-1B remain independently deployable and are unaffected by removal of the non-executable combined stack. The deleted legacy source/configuration/deployment inventory remains recoverable by reverting the focused CLEANUP-1C change from baseline `85a2fa8d512738e6d02ffae42da77af7a27db6fc`. The promoted default and pinned pre-promotion rollback reference remain protected.
- **Backup Script**: `scripts/backup-database.sh` — timestamped pg_dump with configurable retention
- **Deployment Guide**: `docs/PRODUCTION_DEPLOYMENT_GUIDE.md`

---

## Last Changes

- August 12, 2026: SIM-TRUTH-1 completed through backend PR #231, merged `77976a7818695cfdccdea253794acc0cd3c79b91`. Frontend contents are unchanged at `b91ebdba613ca0d8f01a5fb33de355ae97588601`, because the slice is backend-only. `PLAYTEST-TRUTH-1` recorded `simulation/agents/PlaytestAgent` as a second, separate report carrying its own score and deliberately did not fix it; an audit confirmed the same class of claim, live on four mounted routes. `engagementScore` weighted `loopProgress*40 + mechanicsCoverage*30 + npcRate*15 + (engaged ? 15 : 0)`, where the engine sets `loopProgress` from the identical expression the agent recomputed as `mechanicsCoverage`, so one quantity carried seventy of the hundred points; `engaged` was initialised to `true`; and a blueprint with no NPCs scored full marks for interacting with nothing. Blueprints were also penalised for the simulator's own schedule — mechanics visited every third tick and NPCs every fifth, so a blueprint declaring three mechanics or five NPCs could never have them all reached, and the shortfall was reported as a defect in the game. `/lifecycle/tick` substituted `?? 70` for every missing signal, so a game nothing had ever simulated was assessed at composite 73 with a retention figure and a stable trend, and that number selected an evolution branch that patched the blueprint. **The completion is bounded to seven things**: simulation evidence separated into observed, derived and `player: { status: "not-observed" }`; no aggregate engagement score and no grade under any name; simulator-caused shortfalls attributed to the simulator's schedule; client-supplied lifecycle values kept as client claims; missing lifecycle evidence left missing; lifecycle abstaining instead of fabricating health or evolution; and **no real player engagement, retention, fun, quality or runtime measurement claimed anywhere**. Review found thirteen issues, all fixed, including an untrusted-input route with no decoder, a zero-mechanic blueprint wrongly blamed on the simulator, and a rewritten NPC rule that could never fire. Eight invariants were verified by mutation. **Known limitation, preserved rather than resolved**: no server-owned simulation, economy or world evidence source is wired to `/lifecycle/tick` — all three arrive in the request body — so the route always reports insufficient evidence, never evolves, and leaves a game in `CREATED`, saying so explicitly instead of advancing on a default. `PLAYTEST-2`, `PLAYTEST-3` and `PLAYTEST-4` are untouched and remain `unscoped`, and Studio acceptance stays paused and unchanged.

- August 12, 2026: PLAYTEST-TRUTH-1 completed through backend PR #229, merged `63221e79854be718e75a8582b6a7afe248ecb22a`, with Frontend PR #47 `0a6857ad548b9c3d48019dfa7a63adbb5fde4930` and Frontend PR #48 `b91ebdba613ca0d8f01a5fb33de355ae97588601`. `PlaytestEngine` averaged six numbers into an `overallScore` and labelled anything above eighty `production_ready`; one of the six started at eighty and added five points when the generated source contained the substring `pcall`, and it read no findings at all. Nothing in that pipeline has ever run a Roblox play session. The repair loop then decided whether to run, and declared itself complete, by comparing that number to a target. The findings were real and are kept; the arithmetic on top of them is gone. A report now states what produced it — `evidenceKind: "static-analysis"` — and states that runtime quality was not measured rather than leaving it absent or zero, because an absent field reads as an oversight and a zero reads as a failing grade when the question was never asked. A terminal autonomous session reports `qualityScore: null`, and repair decides on outstanding findings. Review found seven defects, three of them boundaries where the slice asserted more than it enforced. A session persisted before the slice carried the old number and its playtest phase is never rerun on recovery, so loading or recovering republished the heuristic as current measured quality; it is now demoted to `legacyQualityScore` and the live value stays `null`. Storage decoding checked the version, the evidence kind, `runtime.status` and the presence of an `issues` array and then cast, so a truncated row returned as a whole report and threw at the caller on first access; every required field is now validated. And `RepairSessionState` required `findingCounts` while documenting legacy rows that omit it, type-checking only because persistence cast — legacy and evidence-versioned sessions and iterations are now discriminated variants, so a legacy absence of findings never reads as zero findings. The recovery invariant, the strict decoder and the version guard were each verified by mutation. **This removes a fabricated measurement; it adds no capability.** Nothing runs a play session, observes a player, simulates input or measures anything at runtime, and the platform still cannot say whether a generated game is good. `performance` remains an estimate derived from counts with nothing timed, each system's `status` restates its finding counts and is not a magnitude, and legacy heuristic numbers stay readable on historical records and are compared to nothing. `PLAYTEST-2` is untouched and remains `unscoped` strategic direction.

- August 11, 2026: ASSET-FABRIC-1 completed through backend PR #227, merged `042ca72d2b6f34af05779bcf8601f441ae93c0c9`. `asset_planner` runs on every generation, its output was stored durably and shipped to Roblox Studio, and nothing anywhere checked it — no schema, no decoder, no validation, and no entry in the artifact dependency rules, so the plan asserted no derivation from the design it exists to serve. `STUDIO-2F-A` had given UI a typed materializable tree and `WORLD-1A` had given the world a typed model; assets had neither. The plan now has a versioned contract in which the kind of an asset is stated on the entry rather than implied by the array it sat in, ids are unique across the whole plan rather than per kind, and an animation's model is referenced by id rather than by the display name the producer used — a target naming no model, or naming two, resolves to nothing and is reported rather than guessed. `purpose` and `required` are recorded honestly rather than filled in: `purpose` is populated for model entries from the producer's `description` and is absent for textures, sounds and animations, whose current producer shape exposes no equivalent field. `required` is representable and unpopulated for every kind, because nothing the producer emits signals required-ness at all. Lineage binds the stage to `GAME_DESIGN`, which is what the pipeline definition already declared for this agent, and it is required rather than merely permitted. Validation is deterministic, runs before persistence and is **advisory**: a malformed plan is stored exactly as the agent produced it with no schema version, so nothing reads it as valid, and the generation still passes because a malformed manifest is not a broken game. Review found seven defects. The worst was that the registered production prompt asked for a shape the contract rejects, so with any provider configured every plan would have been invalid and only the no-LLM fallback could ever have satisfied it — the contract had been validated against its own fallback and would have shipped unreachable. Repair also broke outright on the new required lineage rule, losing the repaired Lua before it was persisted, and the repair playtest stopped seeing assets entirely. **This is the contract only.** Nothing resolves, uploads or materializes an asset; `source: "marketplace"` is recorded as a claim with nothing behind it; and `ASSET-FABRIC-2` remains blocked on the Roblox Open Cloud credential surface that does not exist.

- August 11, 2026: NOVELTY-2 completed through backend PR #225, merged `78c316db594795fa361330301e30933cbbcef2c3`. `NOVELTY-1` measured the structure of each generation and recorded it durably, and nothing read the result — a grep for the stage outside its own producers returned the executor branch, the repair path, the recorder and the constants, and no route, service, execution field or read model. That evidence now has a consumer. A generation carries an explicit verdict **when the run produced enough evidence to form one**: `GenerationExecution.novelty` is optional and absent whenever no `GAME_DNA` artifact was recorded, the report could not be read, or the comparison failed — the same three points at which `resolveNovelty` returns nothing rather than a guess. **Absence means the question was not resolved, never that the generation was found `distinct`.** **Only exact fingerprint equality counts as a repeat.** Two generations with the same digest have the same canonical structure, so calling them the same is a measurement; everything below that is a tuned number, and nothing has measured one — a threshold chosen now would be the defect the platform audit named in `PlaytestEngine`, which awards five points because the source contains `pcall`. There are four verdicts rather than three because three cannot be told truthfully: a repaired execution carries its parent's design forward unchanged, so calling it `duplicate` asserts an unrelated repeat while calling it `distinct` asserts that no prior shares its structure when one does and the reason is known. `repair-preserved` names that case, and repair ancestry is resolved by walking artifact lineage rather than by reading the `-repair-` execution id, which is a naming convention nothing enforces. The `NOVELTY-1` outcome travels on the record and is never collapsed into the verdict, so `no-prior-generations`, `prior-without-dna` and `comparison-failed` remain three distinguishable facts. Review found two defects. The `repair-preserved` verdict was unreachable in production, because `RepairEngine` writes no `GenerationExecution` and the only production caller was the generation path, where an execution has no repair ancestors by construction — every test drove the ancestry walk directly and so none of them caught it; the repair session record now carries its own verdict. And ancestry read only the first Lua artifact for an execution, while the store permits several. **The verdict is advisory.** Nothing blocks, warns, retries or regenerates on it, a duplicate generation still passes validation, and the eight promotion criteria in [NOVELTY-2_PROMOTION_CRITERIA.md](./NOVELTY-2_PROMOTION_CRITERIA.md) — a real-generation fingerprint distribution, adequate sample size and prompt spread, independent human judgements, measured error rates, per-component attribution, the known structural gaps addressed, an override path, and contract tests for the policy — are **all unsatisfied**.

- August 11, 2026: NOVELTY-1 completed through backend PR #223, merged `514c321d794a66e0f6f5d407ba906ab3e45087d1`. The platform already had a diversity mechanism on the live generation path, and it measured the wrong thing. `gameDiversityEngine` seeds a PRNG from the user, blueprint, genre and execution id, picks from five hardcoded pools, and scores the candidate against previous _seeds_ — an instruction the platform invented for itself before any agent ran, so two structurally identical games built from different seeds read as different. Its history is a module-level `Map` holding twenty seeds per user that no restart survives, the similarity it computes is used only inside its own retry loop and reaches no durable record, and after eight attempts it returns a seed that failed its own target with nothing marking it as such. None of that is rewritten here, because seed diversity and output novelty are different questions; the silent fallback is recorded as carried work. What was built is the first thing that compares one generation's structure to another's: a GameDNA derived from the world model and fingerprinted over an explicit sorted encoding, so what is inside the digest can be read rather than inferred, with prior generations read through the storage provider so a restart does not erase them. The fingerprint covers the DNA alone and never the comparison, or the same structure generated twice would fingerprint differently — the one property the slice exists to provide. Two components were deliberately left unbuilt and recorded as gaps: the world model has a single dependency push site, so its graph is always a star into one node and a graph component would carry nothing the role distribution already holds; and progression and economy exist only as one free-text sentence each, so they are fingerprinted as presence rather than shape, because digesting the sentence would move on a reword and hold still on a redesign. Mutation testing found three tests that could not fail and one component that read nothing, all before review. Review then found six more defects, the worst being that a null-agent stage falls through to the generic passthrough — so the v2 pipeline would have persisted `{ _passthrough: true }` as `gameDna.json` under the `game-dna` producer, a durable record claiming a comparison that never ran, while the executor's own comment already said any future agentless stage needs its own branch. **The fingerprint is advisory.** Nothing blocks, warns or alters a generation on similarity, and `NOVELTY-2` is where a gate belongs, advisory before blocking as `SECREVIEW-1` was.

- August 11, 2026: AGENT-SAFETY-1 completed through backend PR #221, merged `f1adac4b9312aa987502c43fd5e32fe47f535ae4`. The first implementation slice from the strategic roadmap set. Its audit separated two things worth keeping apart. No agent can cause a durable side effect: nothing under the agents tree imports the artifact store, a storage provider, the Studio runtime or the database, so the recorder, the executor and the services decide what is persisted, and the platform was already operating at `propose` throughout without ever saying so. The second finding was a real hole. `OrchestratorAgent` read the agents to run straight from its own input and ran them one by one, so whoever shaped that input chose which agents ran — and the three development-tooling agents, which analyse this repository rather than a generated game, were reachable through it. `PIPELINE-1A` had made the generation pipeline server-owned; this was the remaining path taken on trust. Every definition now declares an authority tier and whether it may delegate, and a delegated call is refused before the callee runs and before any provider is reached; the registry builds all sixteen agents in its own constructor, so construction is not what the guard prevents. The platform's own calls stay unrestricted, because only delegation is an agent choosing what runs, and that asymmetry is the model rather than an oversight. Nothing claims `execute`, and validation refuses any definition that does, so the tier cannot be granted by editing a table. Review caught two things: every definition was left at version 1 despite gaining a policy dimension, and the tests accepted any non-`execute` tier so a silent promotion would have passed. Both corrected.
- August 10, 2026: SECURITY-REVIEW-A2 completed through backend PR #216, merged `3e18460c394b03c2d373c7d1a2e9cd0b74b6f984`. The deterministic Luau reviewer reported `clean` whenever no rule fired, with no notion of whether anything had been inspected — and three code paths reached it with nothing to inspect, including a `catch` branch in the v2 pipeline that reviews an empty script list and persists the result as the `SECURITY_REVIEW` artifact. An existing test asserted that behaviour as correct. A report now states an outcome of `pass`, `finding`, `not_applicable` or `not_inspected`, and `pass` is reached only when every supplied script was analysed and nothing fired; a separate `coverageComplete` flag states coverage directly, because a report that found a real defect should still say what it missed. Every reviewed script records the content hash of the exact bytes read, built as the ARTIFACT-CONTRACT-2 construction for a string payload, so a consumer can ask directly whether a report still describes the Lua that is there now. Findings carry a line and the source that raised them, are deduplicated, and are ordered by code unit rather than locale so the same input yields byte-identical output on any host. Four coverage gaps were closed: `OnServerInvoke` was never scanned, so every rule was blind to the RemoteFunction half of the trust boundary; runtime code compilation was unflagged; resolving a player from a client value was read as validation by the general guard; and an outbound request whose target the client chose had no rule. Review found six further defects, the worst being that a package holding an analysed script beside an unreviewable one still passed — which moved the blind spot from the report into a file inside it. **The review remains advisory.** Nothing here blocks generation, delivery or release, and no `SECURITY-REVIEW-B` promotion criterion is satisfied by it.
- August 10, 2026: ARTIFACT-CONTRACT-2 completed through backend PR #214, merged `79b1ecfd137dff71e17fcc57e45078a47137fcdf`. A durable artifact could be stored and delivered but could not answer what exactly it was, who produced it, for which project, or from which upstream state: `pipelineId` named the execution rather than the project, `agent` mixed real agent ids with `null` and with invented strings like `repair-engine`, and no schema version, content hash or lineage existed at all. Every newly produced artifact now carries a schema version, an owning project, a content hash and a producer, and the store refuses to write one that is missing any of those; lineage is recorded only where an upstream actually exists, because an empty dependency list would assert a relationship rather than record one. Content identity is SHA-256 over a canonical serialization — object keys sorted at every depth because the data model treats objects as unordered and a JSONB round trip does not preserve insertion order, arrays left alone because order is meaning there — computed once at creation, and values that cannot be given a deterministic identity are rejected rather than silently dropped. Producer identity is recorded at creation and never re-resolved from the current registry. A dependency binds to an exact upstream hash and is refused on write when it is duplicated, self-referential, cross-project, stage-misnamed, hash-mismatched, or points at an artifact with no durable identity. The concrete defect this closes was live: a repaired execution carried the parent's security review and validation report forward unchanged, so new Lua sat beside a report of the old Lua under one execution id with nothing marking it stale. Review found eleven defects across two rounds, the worst being `Map` and `Set` collapsing to a single content hash and the v2 concept pipeline writing no lineage at all. Historical artifacts are readable and claim nothing, and no database migration was required.
- August 10, 2026: AGENT-CONTRACT-1 completed through backend PR #212, merged `ddfa10453d9e2f48bc9e57045d40a8792d3cc297`. `AgentRegistry` constructed sixteen agents keyed by string, and nothing anywhere stated what any of them produces, whether it may substitute deterministic content, or how many times it retries — an unknown name simply returned `{ _skipped: true }`. Each agent now has exactly one versioned, server-owned definition, and the test of every field was whether something already consults it. `requiresModel` and `fallback` gate `AgentRegistry.executeAgent` before and after the provider call, `reachability` is enforced by pipeline validation, `requiredKeys` is asserted against the parser call sites, and `maxAttempts` is reconciled against what each constructed agent actually loops. Wall-clock timeout, monetary cost and input-token ceilings were deliberately left out and recorded as gaps: `BaseAgent.timeout` is assigned and never read, and `estimateCost` is never called on the generation path, so a contract field would have implied a guarantee the runtime does not make. Provenance lands on `pipeline_steps[].agent_version` only for steps whose agent ran; older executions carry no version and nothing rewrites them. Review found six defects, four of them the contract describing behaviour the runtime does not have, and the reconciliation that catches that class now runs in `npm run validate` rather than only in the test suite. Dynamic orchestration, agent substitution, model routing and cost budgets are not part of this and remain unbuilt.
- August 9, 2026: WORLD-1C is **scoped and blocked**, and no implementation was attempted. The audit of the ownership switch found four conditions that would have to be accepted together, and should not be. The `WorldSceneMaterializer` has never run: there is no Lua execution harness in this repository, so nothing has observed it create an instance, set an attribute or return a path — and WORLD-1C would make it the only source of the world a player sees. Today the Lua world is the safety net: if materialization silently fails nothing is lost, because the generated server script still builds the world, and the switch removes that. The same change necessarily relaxes `getPlayableLuaIssues`, the fail-closed gate reached by generation, the artifact recorder, the v2 pipeline and repair acceptance, replacing its world-creation rules with binding rules. And it invalidates `RUNTIME-PLAYTEST-1`, the only runtime evidence the platform holds, which records an operator-observed Play-mode session over a Lua-created `GeneratedAdventure` folder with a spawn pad and five orbs. The prerequisite is one operator-observed Studio session confirming `WORLD-1B`, which should also close the pending `ARTIFACT-1` and `STUDIO-2F-A` evidence since all three touch the same plugin. The scope record documents the full transition regardless — target ownership, the single-owner invariant, an explicit `worldRuntimeMode` rather than implicit detection, backward compatibility for historical `lua-owned` executions, the rollback hazard where redelivering an older execution into a place holding a materialized world yields two owners, the mode-aware validation contract, repair preserving mode, and artifact atomicity through the staging mechanism `PIPELINE-1B` already introduced. No `worldRuntimeMode` field was added: a contract field with one legal value is scaffolding, and the second value cannot exist until the prerequisite is met. This entry also corrects a pin this session created — PR #210 carried a runtime fix alongside a reconciliation, so the pinned pair named `4fa948b1` while executable code sat at `c42a9b19`. The pins now name `c42a9b19`, and the lesson is that a reconciliation must not carry runtime changes.
- August 9, 2026: WORLD-1B landed through backend PR #209, merged `4fa948b12ebba768ca5e43442dd361d851b905e3`, and is **code complete but not done**. The world model is materialized into Studio as real instances — `Folder`, `Model` and `Part` — under `ReplicatedStorage.AIStudioArtifacts.WORLD_MODEL`. **Design-time only, decided by the audit rather than by preference:** `playableLua.ts` requires the generated server `Script` to build the world into `workspace` at run time, so a scene graph placed there would stand beside a second world on Play, and the creator would see doubled geometry. The live world stays the server script's; the design-time scene is a semantic map beside it. Canonical ownership is `WORLD-1C` and requires `STUDIO-2F-E`. The contract is semantic rather than template-shaped: entities carry a role from the model, zones group by role, transforms derive from ordinals, and relationships travel as attributes referencing other identifiers — there is no spawn, collectible, objective or shop vocabulary in it, and a test fails if a zone name acquires one. Classes are a closed allowlist, so **no script class and no `Source` property can be expressed at all**; the plugin keeps its own copy of every allowlist because it is the side that calls `Instance.new`, and parity tests assert set equality. Identity comes from the model's own entity identifiers, never a random artifact id, so re-export replaces rather than accumulates; only `AIStudioManaged` instances are ever destroyed; creator content found at any depth is preserved first; and the scene is validated whole, built detached, then attached. Receipts pair each semantic entity with its actual instance path, and the backend derives what it expects from the artifact that command carried, so a plugin cannot define its own success criteria. Compatibility is explicit both ways: an artifact without a scene imposes no requirement, and an artifact with one that returns no receipts fails with a message naming the likely cause, because reporting an unmaterialized world as success is the failure the contract exists to prevent. Review found nine defects. Three were P1 and shared one cause: the plugin half was entirely unreachable — the loader never dispatched, the sync manager dropped the receipt, and the packager omitted the module, so a packaged plugin would have failed its own `require` — because three scripted replacements silently matched nothing while the contract tests kept passing, having tested the parts rather than the path. One was critical: creator instances rescued from a zone being replaced were moved into a root that the rollback would then destroy, contradicting the module's own promise that creator content is never destroyed. All nine are fixed and mutation-verified. Evidence limits, stated plainly: there is no Lua execution harness in this repository, so replacement, sweep, collision and partial-attach behaviour is asserted structurally against the delivered source rather than observed running. **The operator-observed Studio session recorded in [WORLD-1B_SCOPE.md](./WORLD-1B_SCOPE.md) is outstanding, and the still-pending ARTIFACT-1 and STUDIO-2F-A acceptance is untouched by this delivery.**
- August 9, 2026: WORLD-1A completed through backend PR #207, merged `18bdc2cddd8b80dba5c779e51c5f3947cce9484a`. Every gate in the pipeline checked one artifact against itself; nothing had ever asked whether two artifacts agree, so a generation could claim a persistence system, ship code that never touches a DataStore, and leave every gate green. A `WORLD_MODEL` artifact now records what a generation claims its world is for, and cross-artifact validation asks whether the generated Lua contains what each claim requires. There is **no new agent**: every derived system, entity and constraint is traceable to a field the design or architecture stage already emitted, with one deliberate exception — the presentation claim, which the platform asserts for every game because the playability contract requires the client to build a HUD. A generation that claimed nothing therefore yields that one claim and nothing else, rather than a model padded with defaults, because padding it would invent claims and then validate them. The model is **deliberately not a template** — claims carry a semantic role (player entry, interactive entity, progress signal, presentation, persistence, server authority, descriptive) composed through relationships, constraints and dependencies, so a racing game, a tycoon and an obby express themselves in the same vocabulary; a model shaped around spawn points and collectibles would describe one game and validate nothing. Three properties matter more than the matching itself: a role nothing in source text could settle is reported `unverifiable` and never `supported`; the evidence map is exhaustive over the role set, so adding a role forces a decision about what would prove it rather than letting it default to a pass; and server claims are matched only against server source, since client code declaring a `SpawnLocation` must not satisfy a claim about what the server owns. Findings are **advisory**, and the model is **non-canonical and unmaterialized** — the runtime world is still built imperatively by the generated server script the playability contract requires, nothing creates Roblox instances, and no ownership switch is proposed. Materialization is `WORLD-1B`, blocked on `STUDIO-2F-E`. Review found four defects, all fixed: the v2 pipeline had no branch for the new stage, so an agentless stage would have persisted a passthrough marker under the name `worldModel.json` — the third time that exact shape has been caught, after the security report and the validation report; comments and string literals counted as implementation, which is the direction of error that turns an unmet claim into a supported one, so source is now normalized with the same helpers the playability contract uses; the leaderstats progress path the contract already accepts was reported unsupported, which would have cried wolf on packages the platform itself considers playable; and a word boundary after an alternation made the `CFrame =` branch unmatchable, so a server placing players that way had its entry claim reported unsupported. Eight mutations were run against the eight regressions, and one exposed a vacuous test of my own — a string-literal fixture that lacked the parenthesis the pattern needs — which is the sixth time that pattern has appeared in this project.
- August 9, 2026: PIPELINE-1B completed through backend PR #205, merged `d7dc84061dd2045e68b4a38f970fe42e7c25abce`. The pipeline has had a `VALIDATION` stage in `StageName` since it was written and had never produced one, so "validation found nothing" and "validation never ran" were the same observation — while two real verdicts were computed and discarded. The playability contract reported by throwing out of the artifact recorder, so its findings survived only as an exception message, and the UI tree builder wrote its failure reason to `console.warn` and nowhere else. `VALIDATION` is now a typed report carrying `schemaVersion`, `analysisMode`, per-check `enforcement` and its own `limits`, so a clean result is never read as proof of quality; it also distinguishes a check that did not run from one that passed. The playability contract stays **blocking** — there is no game without runnable code — and UI materializability is **advisory**, both unchanged from what they already were in practice. **`TesterAgent` was deliberately not wired in**, and that is the load-bearing decision of the slice: it emits a checklist whose every entry is `status: "pending"` alongside `passed: 0, failed: 0`, which stored under the name `validationReport.json` reads as a clean validation result for work that never ran. The v2 pipeline was producing exactly that, so the stage is now deterministic in both pipelines. Conditional stages are not included and are recorded as `PIPELINE-1C`: nothing needs to skip a stage yet, and a branching mechanism with no consumer is scaffolding. One existing invariant had to win over the first implementation: STUDIO-1A holds that a rejected generation persists no content artifacts, because partial content under a failed execution id is what a later consumer could mistake for a deliverable package. Four STUDIO-1A tests caught the first version recording everything and then failing; content artifacts are now staged and committed only once validation passes, so a rejected run leaves exactly one artifact and no generated content — the `VALIDATION` report saying why — and three STUDIO-1A assertions were updated to assert that rather than the old "zero artifacts". Review found seven further defects, four of them the same class the report exists to prevent: a resumed pipeline opens a fresh context session and would have recorded "no Lua generated" for a run holding perfectly good Lua in its saved state; output the contract could not read was reported as missing rather than unreadable; malformed UI output threw past the report entirely through the advisory check that is not allowed to fail a run; and a run whose agents all failed could be labelled `ai`, because every _planned_ node counted as executed. All ten regressions across the slice were mutation-verified — the fix reverted, the test observed to fail, the source restored — and one of those mutations exposed a vacuous test of my own, the sixth occurrence of that pattern in this project.
- August 9, 2026: PIPELINE-1A completed through backend PR #203, merged `d3ddcce4408b950dc2ebe1ef2767998f93e83970`. The generation pipeline was an unexported array inside `PlannerEngine`, and `requiredAgents` from the request body selected against it — a trust boundary, since `POST /api/plan/create` and the v1 plan route both read that field and `/api/plan/execute` then runs the result with real provider calls. Two defects were reproduced against the real planner, executor and registry before anything was changed. A selection that omitted a dependency produced a node depending on a task that was never planned; `getReadyNodes()` requires each dependency to exist, so the node could never become ready and the run ended with nothing done, nothing failed and no reason recorded anywhere — `requiredAgents: ["lua_generator"]` was enough to reach it. A name outside the pipeline became an ad-hoc node, and because `AgentRegistry` answers `{ _skipped: true }` for an agent it does not know while the executor inspected only `_failed`, a plan naming two fictional agents reported success with two completed nodes. The pipeline is now a versioned definition with pure validation: a request may only narrow it, the selection must be closed over its own dependency edges, and it is ordered by the definition rather than by the request, so the same requested set always yields the same plan. An unbuildable selection raises a typed error and the plan routes answer 400 with the issues. The executor no longer counts an agent that never ran as completed work, a blocked node is marked skipped with what it was waiting for instead of staying pending, and the definition id and version are recorded on the execution so an artifact set can be attributed to the exact pipeline that produced it — rows written earlier read back `undefined` and must not be assumed to be the current shape. Review found two more real defects, both fixed: cycle diagnostics missed a member reachable only through an already-visited branch, replaced with strongly connected components and confirmed by running the old walk against the failing fixture, and the pipeline identity was recorded only on the success path, so a run failing after planning lost the one thing already known about it. Every regression was verified by mutation — the fix reverted, the test observed to fail, the source restored. Adding stages to the pipeline is deliberately out of scope: a validation gate and conditional nodes are `PIPELINE-1B`, because adding a stage changes what every generation runs and costs and needs its own evidence.
- August 9, 2026: ARTIFACT-1 (code complete, not done), PROVIDER-1B and SECREVIEW-1 landed through backend PR #199, #200 and #201, merged `2a3fc01a76a59cf7e92233037f96dcb22a9d30c3`, `b1c2283a2d8ae6722b29a7eeca45e177b4eb7ca7` and `12bd4dc6c736aa6af7b3346624152032157d5bd1`. **ARTIFACT-1** made metadata delivery idempotent: instances were named after `artifact.id`, which `ArtifactStore` mints with `randomUUID` on every store, so regenerating a project left the previous run's `StringValue` behind and the stage folder grew by a full set on every export. The stage-derived artifact name is stable and unique within its folder, and instances left by the old identity are cleared by a rule narrow enough that it cannot catch hand-added instances or materialized UI trees. It is **code complete but not done**, on the same standard applied to STUDIO-2F-A: the change is plugin Lua, the repository has no Lua test harness, and the structural source assertions cannot show that a real re-export replaces the instance without destroying creator content. **PROVIDER-1B** closed the remaining hole in generation provenance: PROVIDER-1A marked a stage as fallback only when no provider was configured, but deterministic content also enters after a successful call — an unparseable response substitutes the whole canned object, a missing required key is repaired from it, and `LuaGeneratorAgent` substitutes a safe repair when the model's Lua fails the playability contract. All three routes now declare themselves, so an execution is `ai` only when the model authored everything. **SECREVIEW-1** added a deterministic Luau trust-boundary review recorded as a `SECURITY_REVIEW` artifact. It is **advisory by explicit decision**: a finding never negates generation, delivery or release, the artifact carries `analysisMode: "deterministic-pattern"` and `enforcement: "advisory"` as typed values on the durable record so a later regime cannot be read backwards onto it, and a blocking gate is the separate `SECURITY-REVIEW-B` delivery gated on seven documented criteria. Review across the three slices found nine real defects, all fixed before merge, and two classes are worth recording because they recurred: a value quietly lost at a layer boundary (the metadata sweep destroying what it had just written; the v2 pipeline persisting `{ _passthrough: true }` under the name `securityReport.json`), and a test that passes with and without the fix (two SECREVIEW-1 loop regressions passed with the defect deliberately reintroduced, because a different fix suppressed the symptom). Both were caught by mutating the source and confirming the test fails before restoring it. Evidence limits, stated plainly: the security review is pattern analysis over source text, not a Luau parser or dataflow analysis, it follows a client value through at most one direct assignment, and a clean report is not proof of safety — the report says so itself. The PROVIDER-1B acceptance run used a real local Ollama on `qwen2.5-coder:7b` but with in-memory storage on an isolated port, so it wrote nothing to a production database, and no Studio-attached run was performed for any of the three.
- August 9, 2026: STUDIO-2F-A code complete but not done (backend PR #195/#196/#197), merged `d7e444549d324df9df339ff5bda7a75a820e39e3`. Generated UI now reaches Roblox Studio as a real, selectable, editable `ScreenGui` tree under `ReplicatedStorage/AIStudioArtifacts/UI_GENERATION/` instead of an inert JSON string — the first non-Lua artifact class to materialize as instances. PR #195 added the closed wire contract (`UIInstanceTreeContract`) and a deterministic builder in which the model supplies intent while geometry, `ZIndex` and `LayoutOrder` are derived in code; PR #196 added `UITreeMaterializer.lua` and moved the plugin to v1.9.0; PR #197 added backend verification of the reported screen set. Two format decisions are load-bearing rather than stylistic: the payload carries no identifiers at all, because `ui-gen/types.ts` and `ArtifactStore` both mint `randomUUID` and a leaked id would make byte-identical designs hash differently and re-deliver forever, so parenting is expressed by nesting and screens are keyed by name; and every property value carries an explicit `kind`, with `color3` as 0–255 integers so the JSON round-trips exactly. The class, property, value-kind and Enum allowlists are a security control held independently on both sides, since the plugin is the side that calls `Instance.new`; no script class and no `Source` property appears in either, and a cross-language test asserts the copies stay identical. Review found four real defects across the three PRs, all fixed before merge: replacement and sweep destroyed creator-authored instances nested below the first level, because only direct children were rescued; `SyncManager` and then `parseImportReport` each silently dropped the identity-bearing screen receipt, the second of which would have made verification reject every real UI export; and verification read the mutable artifact store rather than the content the export actually carried, which `ProjectSyncManager.applyChange` can change after queueing. Evidence limits, stated plainly: coverage is backend-only. `studio-plugin/` has no Lua test harness and the plugin assertions are structural checks over source text, so **nothing in this phase has executed Lua**. Operator-observed Studio acceptance is outstanding and is required before the delivery path can be called proven; the five required observations are recorded in [STUDIO-2F-A_SCOPE.md](./STUDIO-2F-A_SCOPE.md). The imperative Lua HUD remains runtime-canonical by the scope's binding decision, so the delivered tree is inspectable and editable but inert at Play time by design; flipping that is `STUDIO-2F-E`. Backend receipt verification also does not apply to an artifact absent from the queued payload, because the expected screen set cannot then be computed; its hash is still checked.
- August 8, 2026: PROVIDER-1A completed through backend PR #193, merged `ecae9bb59f1639d2e382e7221a956aa96460b3e9`. It is backend-only and is the first executable change to advance the runtime pair since REPAIR-1; the Frontend identity is unchanged, confirmed by the paired production contract staying green against the existing pin. A repository audit found three defects that composed into one truthfulness failure: `LLMProviderFactory.createByName()` handled only `openai`, `anthropic`, `gemini` and `groq`, so `DEFAULT_PROVIDER=ollama` or `openrouter` fell through and silently returned `provider: null`; every agent guards its own LLM call and returns a deterministic fallback when none is wired; and `LuaGeneratorAgent`'s fallback is a complete hardcoded game deliberately written to satisfy `getPlayableLuaIssues()`. A misconfigured deployment therefore produced a green pipeline, a Studio import with valid receipts and a positive playtest score for a game unrelated to the user's prompt, with nothing durable to distinguish it from a real generation. The slice adds explicit `ollama` and `openrouter` selection, rejects unknown provider names by name, marks an unsatisfiable explicit request with `requested`/`unsatisfied`/`unsatisfiedReason` instead of degrading silently, adds the default-off `REQUIRE_LLM_PROVIDER=true` startup refusal — which fires whenever no provider resolved, covering an empty configuration as well as an unconstructable explicit request — and records optional `ai_mode`/`ai_provider`/`ai_model` on every `GenerationExecution`. Provenance fails safe: an execution is `ai` only when a provider resolved and no stage returned canned content; pre-existing rows read back `undefined` and are reported as unknown, never as `ai`. Storage is the existing JSONB `kv_store`, so no migration is required. Only provider and model names are recorded — never keys, endpoints or prompt bodies. Verification of the forced path initially appeared to fail with a "no API key found" startup line; that output came from a checkout without the fix, and forced Ollama was confirmed keyless on the fixed branch, logging `Ollama (forced, url: http://localhost:11434, model: qwen2.5-coder:7b)`. Two regressions now pin that behavior explicitly: one deletes every provider credential and asserts forced Ollama still resolves with a startup line that never mentions an API key, and one walks `openai`, `anthropic`, `gemini`, `groq` and `openrouter` asserting each still fails closed without its credential and resolves with it, so the keyless path cannot be read as globally relaxed credential validation. Pre-merge review found two real defects in the slice itself, both fixed before merge: the startup refusal was gated on `unsatisfied`, which is set only for an unconstructable explicit request, so a release image that lost its provider configuration entirely would still have started and served canned content — the exact misconfiguration the control exists to catch; and the `ai_mode` documentation described an `and` condition where `resolveProvenance` uses `or`, contradicting a passing test. The startup decision now lives in the pure, exported `shouldRefuseStartupWithoutProvider` rather than inline in `index.ts`, where it was untestable and therefore shipped wrong. Evidence limits, stated plainly: verified by typecheck, lint, architecture and boundary validators, 1061 passing backend tests, 34 provider-selection tests and 5 provenance tests; the one failing suite (`persistence.test.ts` DB health) asserts an unreachable database and fails only on a machine that has a live PostgreSQL, and imports nothing this slice changed. No Studio-attached live run of a provider-backed generation was performed for this slice. `REQUIRE_LLM_PROVIDER` is not yet enabled in the release image, and the auto-detection path still defaults to the hardcoded `llama3` model; both are recorded as remaining debt rather than silently changed.
- August 8, 2026: `STUDIO-2F` was decomposed and `STUDIO-2F-A` scoped — see [STUDIO-2F-A_SCOPE.md](./STUDIO-2F-A_SCOPE.md). Two findings drove this. First, the item was never blocked by an outstanding control: the "control gates" wording was genericized on August 8 precisely because the last named blocker (artifact-applying repair evidence) had completed, and every gate the historical planning documents name as a precondition is complete — so `STUDIO-2F` was blocked by not being scoped. Second, it cannot ship as one sprint, because [ROADMAP_RULES.md](../governance/ROADMAP_RULES.md) requires exactly one objective per sprint and the only prior breakdown was a five-bullet sketch in a non-authoritative audit document. `STUDIO-2F-A` (generated GUI materialization) is scoped; `2F-B` (native assets), `2F-C` (place/`.rbxl`), `2F-D` (runtime validator packaging) and `2F-E` (canonical-HUD flip) are named for ordering only and remain unscoped. The scope locks in one binding decision: the imperative Lua HUD stays runtime-canonical and `assertPlayableLuaScripts` is not modified, with the materialized GUI tree parented under `ReplicatedStorage/AIStudioArtifacts/UI_GENERATION/` rather than `StarterGui`, because relaxing that gate would leave `RepairExecutor` accepting repairs against an invariant it structurally cannot evaluate, and because a partial export does not roll back already-created instances. Scoping only — no runtime code changed.
- August 8, 2026: the REPAIR-1 Frontend UI completed the phase end to end — Frontend PR #40 (`6c1458d836244f2b720f361a78c2ab13f1682f74`) adds an Integrate-stage repair panel that runs an artifact-applying repair against the latest generation execution, redelivers the repaired execution to a connected Studio session, rolls back to a project-validated earlier execution, and reads the durable delivery audit trail. It adds no backend change and spans two route generations: the run action uses the REPAIR-1A route `POST /api/repair/run`, while redelivery, rollback and the audit trail use the REPAIR-1B/1C routes `POST /api/repair/:projectId/deliver` and `GET /api/repair/:projectId/deliveries`. It also repaired a stale client contract: the previous generic repair call predated REPAIR-1A and posted script/asset payloads without the `executionId` the merged route requires, so it would have failed closed with HTTP 400 on every invocation. CodeRabbit review found one real defect before merge — a mutation begun on one project could resolve after a project switch and write the previous project's session and delivery history into the panel, because it reused a `reconcile` closure bound to the earlier project and superseded the current project's in-flight request through the shared latest-request guard; a dedicated mutation guard fixes it. The paired release advances to backend `ccd28ef816d1653df0aebd0775f70187aa321564`, which is runtime-identical to the REPAIR-1C merge (`558f9e6f5cc80e3ae9e29cafdd15ce6a33addd0f`) because every commit between them is documentation-only, verified by diff. Evidence limits, stated plainly: the panel is covered by contract, parser, type, build and paired-contract CI, but no Studio-attached live end-to-end run of the UI was performed, and the Frontend workspace test runner cannot load `.tsx`, so neither the panel nor its guard fix has component-level regression coverage.
- August 8, 2026: REPAIR-1C completed — backend PR #189 (`558f9e6f5cc80e3ae9e29cafdd15ce6a33addd0f`) added a durable `RepairDeliveryRecord[]` audit trail on the existing repair session, an optional `executionId` on `POST /api/repair/:projectId/deliver` that lets an operator roll back to an explicitly validated older execution (the original parent or an earlier repair, drawn only from that project's own history — never an arbitrary client-supplied id) instead of only the latest repair, and `GET /api/repair/:projectId/deliveries` combining that audit trail with the existing generic Studio evidence. A design review before implementation found and fixed two real bugs up front: `recordDelivery` now joins the same per-project serialization queue `run()` already uses (otherwise a concurrent run/delivery could silently drop one write), and a later `run()` call now carries the delivery audit trail forward instead of erasing it. Post-review, CodeQL repeatedly flagged log-injection risk in the audit-write-failure logging — two different sanitizer approaches (a regex helper, then a `JSON.stringify` helper) were both invisible to its taint analysis because it doesn't do interprocedural summarization through named local functions; the fix was inlining the sanitizing `.replace()` call directly at each log call site. REPAIR-1 (1A/1B/1C) is now complete on the backend; a Frontend UI is a separate follow-up.
- August 8, 2026: REPAIR-1B completed — backend PR #187 (`6ec42d55a74bab0a9001d7e66c02795f01b41886`) added `POST /api/repair/:projectId/deliver`, which resolves the latest repaired execution server-side from the repair session's own history and reuses the existing `StudioIntegrationManager` sync pipeline unchanged to deliver it to a connected Studio client; no Studio plugin or Frontend changes were needed. A design review also surfaced and fixed a real bug in `RepairEngine.run()` (session history was overwritten instead of appended across calls), plus two CodeRabbit findings fixed in a follow-up commit before merge: a concurrent-run race and an execution-id collision between two successful repairs of the same parent. Studio redelivery is now real end-to-end; rollback/audit and a canonical-execution UI (REPAIR-1C) remain.
- July 31, 2026: FRONTEND-2C completed through protected quality and cleanup (PR #18), production bundle budgets (PR #19), Lucide import hygiene and tightened budgets (PR #20), and authentication/realtime recovery coverage (PR #21). Canonical `Frontend/main` is `022788ace31982e2b08ea099800de784b4dbe482`; Frontend CI #140 passed all 10 jobs with 22 workspace tests and the protected 40-check backend contract. REL-203 issue #144 promotes this exact source into the backend release pairing.
- July 31, 2026: DATA-202 completed through pipeline persistence (PR #137), autonomous session/checkpoint persistence (PR #139), and Studio operational evidence plus runtime ownership classification (PR #141). The final protected source head `010532f0b162097c8a645b1dc07c89081d25cb99` passed CI Pipeline #1073 and merged as `a33a8c30588f1e4705d27856e61d839c8efd42ac`. Compatibility-write inventory remains zero; 23 operational runtime owners are explicitly classified. The exact Frontend contents remain `9495b696cf22c84cf61375f7df22e5ac5907cc3c`.
- July 31, 2026: REL-202 records the current DATA-202 release candidate, immutable CI artifact digests, preserved rollback reference and the boundary between verified CI composition and an unperformed external deployment. Issue #142 tracks the documentation-only closeout.
- July 28, 2026: HARDEN-2A / DOC-201 synchronized the active authentication and two-repository deployment guides, linked their claims to native/composed/protected evidence, annotated the conflicting July 16 decision records as superseded, and expanded the auth-contract test into a search-based authority guard. Issue #49 tracks the focused documentation-only implementation.
- July 28, 2026: HARDEN-2A / INT-201 completed in both repositories. Frontend PR #16 merged as `739b43cbc5f991c1852e80b30fe38c0e7c02d681`; backend PR #48 merged as `b30be04ce3c5458902561472d371f753b28f08c5` after its CodeRabbit supply-chain finding was fixed with read-only job permissions, non-persisted checkout credentials, and a single inventory-backed Frontend pin. Backend post-merge run #307 passed all protected jobs and 40/40 production checks. Contract artifact `8684538568` has digest `sha256:0babbaf1239615e15479a4adbbcc5f5745965632fb3417c06bc2ee9a70c3c0a9`.
- July 28, 2026: HARDEN-2A / FE-201 merged through Frontend PR #14 as `2aab7c3367bb55520edc2576422ebafec265d7c6`. The Workspace now parses and renders verified, pending, failed, malformed, and disconnected Studio status instead of hardcoding verification false.
- July 28, 2026: HARDEN-2A / SEC-201 removed reusable credentials from register/login/refresh JSON while preserving user/role metadata and httpOnly cookie auth. Refresh credentials are now 256-bit random values persisted only as SHA-256 digests with direct digest lookup, migrated from legacy plaintext records before startup traffic, and consumed before replacement so replay fails. Four native production-contract tests cover body shape, cookie policy, digest-only storage, migration, rotation, replay rejection, `/auth/me`, and active terminology. The composed HTTPS verifier now checks register/login/refresh body safety and rotation alongside REST and Socket.IO. Local verification passed `npm run ci` (62 test files passed, one skipped; 676 tests passed, one skipped), the production backend build, all 1,096 tracked-path invariants, and the pinned Frontend workspace tests/build. Issue #45 is closed as completed.
- July 28, 2026: TECH-AUDIT-2 established the first official backend + standalone Frontend baseline at backend `a2f596dcb03d92791f96d1b217bf33a534eeddcb` and Frontend `1036c3ef9705d145cb9700cd14268a33d2abdd58`. The audit inventories 46 backend subsystems, the 99-file Frontend TypeScript surface, and the 14-source Studio plugin; reconciles historical completion claims; records 16 prioritized debt items; and orders HARDEN-2A → ARCH-2B → FRONTEND-2C → RUNTIME-2D → DURABILITY-2E → STUDIO-2F. Seven deliverables live under `docs/02-audits/technical-v2/`.
- July 28, 2026: The schema-v5 steady-state cleanup guard merged through PR #43 as `a2f596dcb03d92791f96d1b217bf33a534eeddcb`. Post-merge CI run `30338196639` (#298) passed and artifact `8680037918` (`post-removal-invariant-audit`, digest `sha256:c7e908d07c64c8e6b9b5cf654d94501dbe9a25d85162e7c4d5638cab9e1528bb`) preserves historical cleanup proof without freezing future reviewed changes.
- July 28, 2026: CLEANUP-1D completed through PR #42 and merge commit `f924079995059d9b86a5caaaf6364cb7b4879881` with zero file deletions. All six review findings were resolved in follow-up commit `5a7d9c84f8aabf6618684ecfdd77bc176283a925`, including traversal-safe path classification and stateless import guards. Pre-merge CI run `30336635283` (#295) and post-merge push run `30336910264` (#296) passed every applicable job and Merge Gate. Post-merge evidence artifact `8679552100` (`cleanup-1d-post-removal-verification`, digest `sha256:a8fa2cea2192b4f69471926521678307a0409e3a053c699f5783fdbfb04aa14c`) confirms 26 historical implementation paths, zero deletions, all 176 removed paths absent, all 12 removed packages without consumers, 1,088 deterministic tracked paths, and zero release-isolation violations. Issue #41 is closed. The schema-v5 guard now verifies these invariants without freezing future repository changes. See `docs/project/CLEANUP-1D_POST_REMOVAL_VERIFICATION.md`.
- July 28, 2026: CLEANUP-1C completed through PR #39 and merge commit `1bc54753783610827750dbb11689c0fb24620923` — 168 root `src/` files, five frontend entry/config files, and three archival combined-deployment files were deleted (176 paths total). Five runtime and seven development-only direct package declarations were pruned; `socket.io-client` remains because the active composed-release verifier consumes it. The existing production-readiness audit now checks the canonical Studio plugin instead of removed `src/shared`. Final PR CI run `30329991563` (#290), Studio Plugin Package run #27, and post-merge push run `30330505927` (#291) passed every applicable job and Merge Gate. Post-merge evidence artifact `8677218198` (`cleanup-1c-physical-removal`, digest `sha256:b7693a9fac74b6e6615b5ba277e98ccb8575d21147e381d9ebcb5e400c89f74f`) confirms the exact removal, synchronized root lockfile declarations, zero remaining package consumers, and active-release isolation. Issue #37 is closed; CLEANUP-1D is next. See `docs/project/CLEANUP-1C_PHYSICAL_REMOVAL.md`.
- July 28, 2026: CLEANUP-1B completed through PR #35 and merge commit `703fe0fbcfcb8706506e9351af1fe7874a1337f0` — root development/build/typecheck commands now target the backend, Vitest uses an explicit backend-only configuration, protected TypeScript and PostgreSQL CI reuse those commands, and architecture reporting identifies the standalone Frontend as canonical. PR CI run #282 and post-merge push run `30327587217` (#284) passed every applicable job and Merge Gate. Post-merge evidence artifact `8676270323` (`cleanup-1b-tooling-decoupling`, digest `sha256:5d14de17c49751392d009021dc9a7b14605447ae72d44e24414848d3331169ec`) confirms the exact 168-file legacy inventory, unchanged dependency maps and lockfile, active-release isolation, and `currentStageDeletionAuthorized=false`. Issue #34 is closed; CLEANUP-1C is next. See `docs/project/CLEANUP-1B_TOOLING_DECOUPLING.md`.
- July 28, 2026: CLEANUP-1A merged as `99b0de4a3b493d1e1fa98173deea8fae59d57842` — the audit classified all 168 tracked root `src/` files, package consumers, deployment inventory, tooling blockers, and ordered CLEANUP-1B/1C/1D waves without modifying legacy runtime files.
- July 27, 2026: CUTOVER-1C verified — backend head `8bee44a284244033d73637b3e3cc4bddf72af035` composed the exact Frontend release commit `1036c3ef9705d145cb9700cd14268a33d2abdd58` behind one HTTPS origin. CI run #219 and evidence artifact `8670986116` proved healthy services, SSR, CORS allow/deny behavior, secure host-only cookies, authenticated REST, unauthenticated Socket.IO rejection, and authenticated polling → WebSocket upgrade. The prior release stack and legacy frontend remain preserved for rollback. See `CUTOVER-1C_COMPOSED_RELEASE.md`.
- July 27, 2026: CUTOVER-1B completed — Frontend PR #12 merged as `1036c3ef9705d145cb9700cd14268a33d2abdd58`. One shared worker-to-Node adapter now serves both responsive QA and production. Frontend CI run #65 verified the non-root image, independent `/health`, root SSR document, desktop/tablet/mobile responsive QA, and Merge Gate. CUTOVER-1C is the next gate.
- July 27, 2026: CUTOVER-1A completed — backend PR #22 merged as `22852af9d4db30c4fff28ea5ef0c762aa3c0607a`. The backend-only production image builds without legacy frontend sources and passes its container health smoke gate.
- July 27, 2026: STUDIO-1g real desktop acceptance completed — standalone Frontend commit `a8d005d433d48e18d8e64ac176ee63c9c694b644` generated execution `exec-1785180356168`; plugin v1.8 on Roblox Studio `0.730.0.7300790` imported eight artifacts, created the expected Roblox instance hierarchy, returned exact receipts, and reached `Verified`. The authenticated project and session status matched project `proj-286c6929-5`, client `studio-39fa03bb`, session `session-afec81df-c`, command `cmd-96bd9df4-e`, and eight verified artifacts. Issue #15 is closed. See `STUDIO-1G_DESKTOP_ACCEPTANCE_RESULT.md`.
- July 27, 2026: STUDIO-1f desktop findings resolved — PR #16 removed the Roblox-forbidden custom `Content-Type` header while retaining the canonical connector and protocol. PR #17 normalized the exact current `LuaGeneratorAgent` output at the existing `GenerationArtifactRecorder` boundary and rejects empty, malformed, or duplicate Lua artifacts before queueing. PR #19 preserved canonical specialist assignments when adaptive performance evidence is absent. Subsequent real desktop acceptance passed in STUDIO-1g.
- July 25, 2026: STUDIO-1e acceptance packaging verified — `npm run studio:package` now creates a deterministic installable `.rbxmx`, source/bundle manifest, and SHA-256 checksum file from the explicit active plugin allowlist. The package workflow parses the Roblox XML model, verifies checksums, and publishes a downloadable artifact. The standard test suite proves repeat builds are byte-identical, and the desktop runbook defines installation, status capture, evidence fields, and failure triage.
- July 24, 2026: STUDIO-1d canonical plugin implementation verified — existing plugin modules now use the correct dependency graph and backend project ID, poll and acknowledge `EXPORT_PROJECT`, materialize structured Lua scripts and non-Lua metadata as real Roblox instances, report exact ID/hash receipts, and expose Verified only after backend evidence acceptance. The plugin source contract is enforced by the normal test suite.
- July 24, 2026: STUDIO-1c backend slice complete — the shared Studio runtime now enforces polling → acknowledgement → result ordering, validates the exact durable execution ID and artifact ID/SHA-256 receipt set, records verified/failed session states, and exposes accurate additive project status fields. PR #10 delivered the contract; PR #11 restored canonical CI, removed temporary diagnostics, completed formatting, and passed every standard validation gate. See `STUDIO-1C_IMPORT_ACKNOWLEDGEMENT.md`.
- July 24, 2026: STUDIO-1b complete — project and plugin Studio routes now share one v2 runtime; project sync selects the newest completed artifact-bearing execution, queues real persisted artifacts through `EXPORT_PROJECT`, preserves incremental no-op behavior, and never creates placeholder Lua/config packages. PR #9 passed TypeScript, ESLint, Prettier, 717 tests, repository validation, commitlint, PostgreSQL restart E2E, and Merge Gate. See `STUDIO-1B_RUNTIME_CONSOLIDATION.md`.
- July 24, 2026: STUDIO-1a complete — canonical `PlanExecutor` outputs are recorded in the storage-backed `ArtifactStore` under durable generation execution IDs and survive PostgreSQL provider reconstruction. See `STUDIO-1A_ARTIFACT_LINEAGE.md`.
- July 24, 2026: WORKSPACE-1 complete — the standalone project route now uses five workflow stages, one persisted read model, stage-scoped advanced tools, typed context and result presenters, native workflow regression tests, and production-artifact responsive browser QA. Responsive QA found and fixed document-level chat scrolling, workflow shrink issues, and mobile manifest overflow.
- July 24, 2026: CORE-1b verified — blueprints, versions, generation executions, conversations, and messages now use the configured storage boundary. GitHub Actions passed TypeScript, lint, formatting, full tests, repository validation, commitlint, real PostgreSQL restart recovery, ownership isolation, and the merge gate. See `CORE-1B_DURABLE_RUNTIME.md`.
- July 24, 2026: CORE-1a implemented — one configured storage provider now backs identities, users, projects, generation history, and API keys; project ownership is mandatory and browser patches are allow-listed. See `CORE-1A_DURABLE_PROJECTS.md`.
- July 24, 2026: CUTOVER-0 and CI-BASELINE-1 merged — governance and portable CI are complete on the active backend integration branch; the standalone frontend CI is merged on `main`.
- July 16, 2026: Autonomous Pipeline real-time integration complete — events emitted via PipelineEventEmitter, AgentBoard shows phases live, Socket.IO primary transport with polling fallback, step.failed handling added.
